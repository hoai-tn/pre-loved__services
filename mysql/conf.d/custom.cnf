[mysqld]
# Note: default_authentication_plugin is not supported in MySQL 9.0+
# MySQL 9.0 uses caching_sha2_password by default
# This configuration helps with JDBC connections from Logstash and other applications

[client]
default-character-set=utf8mb4
