input {
  tcp {
    port => 5044
    codec => json
  }
}

filter {
  mutate {
    add_field => { "app" => "pre-loved" }
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "pre-loved-logs-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
